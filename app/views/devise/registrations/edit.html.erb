<% title "Edit login" %>
<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
      <div class="panel panel-default">
        <div class="panel-heading" id="form_header">
            <h4 class="panel-title">Edit <%= resource_name.to_s.humanize %></h4>
        </div>
        <div class="panel-body new_class">

          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
            <%= f.error_notification %>
            
            <div class="form-inputs">
              <div class="row">
                <div class="col-xs-12 col-sm-6 col-md-6">
                  <%= f.input :fname, label: "First name",  autofocus: true %>
                </div>
                <div class="col-xs-12 col-sm-6 col-md-6">
                  <%= f.input :lname, label: "Last name", autofocus: true %>
                </div>
                <div class="col-xs-12 col-sm-6 col-md-6">
                  <%= f.input :tel, label: "Phone number", maxlength: "11", placeholder: "e.g 08145463102", autofocus: true, required: true %>
                </div>
                <% unless current_user.provider == "Facebook" || current_user.provider ==  "Google" %>
                  <div class="col-xs-12 col-sm-6 col-md-6">
                    <%= f.input :email, required: true, autofocus: true %>
                  </div>
                <% end %>
                <div class="col-xs-12 col-sm-12 col-md-12">
                  <%= f.input :avatar, as: :file, hint: 'Recommended size: 300px <span style="font-size: .8em;">x</span> 300px'.html_safe, label: "Profile Pic" %>
                </div>
              
              
                  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                    <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
                  <% end %>
                <% unless current_user.provider == "Facebook" || current_user.provider ==  "Google" %>
                  <div class="col-xs-12 col-sm-12 col-md-12">
                    <%= f.input :password, label:"New password", autocomplete: "off", hint: "Leave blank if you don’t want to change your password", required: false %>
                  </div>
                  <div class="col-xs-12 col-sm-12 col-md-12">
                    <%= f.input :password_confirmation, label:"Confirm new password", required: false %>
                  </div>
                  <div class="col-xs-12 col-sm-12 col-md-12">
                    <hr>
                    <%= f.input :current_password, hint: "We need your current password to confirm your changes", required: true %>
                  </div>
                <% end %>
              </div>
            </div>

            <div class="form-actions text-center">
              <%= f.button :submit, "Submit", id:"form_submit" %>
              <%= link_to 'Cancel', current_user, class:'btn btn-default', id:"form_cancel" %>
            </div>
          <% end %>
          
          <div style="margin: 20px;">
            <hr>
          </div>
          <span class="text-center">
            <p>Unhappy? <%= link_to "Delete my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, id:"form_drop_down", method: :delete %></p>
          </span>
        </div>
        <% if current_user.avatar.present? %>
  				<div class="panel-footer">
  					<div class="text-center" style="color: #777; margin-bottom: 10px;">
  						<strong>current picture</strong>
  					</div>
            <%= image_tag current_user.avatar.url(:avatar), class:"img-responsive", style: "margin: 0 auto; margin-bottom: 15px;"%>
  				</div>
  			<% end %>
      </div>
    </div>    
  </div>
</div>

